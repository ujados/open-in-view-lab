spring:
  jpa:
    open-in-view: false
    properties:
      hibernate:
        generate_statistics: true
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true

logging:
  level:
    org.hibernate.stat: WARN
    org.hibernate.SQL: OFF
    org.hibernate.orm.jdbc.bind: OFF
